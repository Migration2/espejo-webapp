export const environment = {
  production: true
};
export const DOMAIN = location.hostname
export const URL_WEBSOCKET = `https://${DOMAIN}:4547/bicirio-websocket`;
export const POOLLING_TIME_IN_MILLIS:number = 60000;
